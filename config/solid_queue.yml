default: &default
  dispatchers:
    - polling_interval: 1
      batch_size: 1
      recurring_tasks:
        scoop_round:
          class: ScoopPopJob
          schedule: every 2 minutes
        taylor:
          class: LizardRoundJob
          args: [ 'PornLizardTaylor' ]
          schedule: every 4 minute
        warren:
          class: LizardRoundJob
          args: [ 'PornLizardWarren' ]
          schedule: every 4 minutes
        ki:
          class: LizardRoundJob
          args: [ 'PornLizardKi' ]
          schedule: every 4 minutes
  workers:
    - queues: "*"
      threads: 4
      polling_interval: 0.5

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
