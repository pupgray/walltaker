<%= form_with(model: comment, url: [link, comment], class: 'new_comment_form', data: { controller: 'new-comment-form' }) do |form| %>
    <%= form.text_field :content, autocomplete: 'off', data: { survey: 'input', action: 'input->new-comment-form#inputChange' } %>
    <%= form.submit 'Send', data: { survey: 'send' }, disabled: true %>
<% end %>
