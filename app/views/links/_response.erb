<% target = "link_response_#{link.id}" %>
<%= turbo_frame_tag target do %>
  <% if link.current_past_link && link.response_text %>
    <div class="link--response">
      <strong>
        <%= link.user.username %>
        <% if link.current_past_link.horny? %>
          😍:
        <% elsif link.current_past_link.came? %>
          💦 said while cumming:
        <% end %>
      </strong>
      <%= link.response_text %>
    </div>
  <% end %>
<% end %>