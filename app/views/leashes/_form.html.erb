<%= form_with(model: leash) do |form| %>
  <% if leash.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(leash.errors.count, "error") %> prohibited this leash from being saved:</h2>

      <ul>
        <% leash.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
